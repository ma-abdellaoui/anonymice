{
  "type": "object",
  "properties": {
    "trusted": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Destinations inside our boundary. Plaintext may pass. Enterprise-policy only — deliberately NOT user-editable."
    },
    "tokenizing": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Foreign destinations the extension actively intercepts."
    },
    "vaultUrl": { "type": "string" }
  }
}
