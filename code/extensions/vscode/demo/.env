# Try it: select a value below and run "Anonymice: Tokenize Selection"
# (right-click, or the Command Palette).
#
# The line becomes  DB_PASSWORD=ANM1-SECRET-...  in the buffer, and the real
# value renders beside it as a decoration. Save the file and `cat` it: the
# plaintext is gone from disk too (SPEC §2.4).

DB_PASSWORD=hunter2-prod-9f
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
GITHUB_TOKEN=ghp_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
